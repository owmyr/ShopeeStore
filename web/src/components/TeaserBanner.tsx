"use client";

import React from "react";
import { useVip } from "@/context/VipContext";

/**
 * Returns null to eliminate ad-like banners from the UI, ensuring an uninterrupted,
 * dignified editorial reading experience without aggressive conversion walls.
 *
 * @returns JSX.Element | null
 */
export function TeaserBanner(): React.JSX.Element | null {
  return null;
}
